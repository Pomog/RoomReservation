{{template "base" .}}

{{define "content"}}

    <div class="container">
        <div class="row">
            <div class="col">
                <h1>About</h1>
                <p>This is the about page</p>

                <p>This is from the Template : {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>

{{end}}